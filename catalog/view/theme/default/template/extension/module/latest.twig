<div class="model-title">B НАЛИЧИИ</div>
 {% for product in products %}
  <div class="product-layout col-lg-3 col-md-3 col-sm-6 col-xs-12">
    <div class="product-thumb transition">
      <div class="image"><img src="{{ product.thumb }}" alt="{{ product.name }}" title="{{ product.name }}" class="img-responsive" /></div>
      <div class="caption">
        <h4>{{ product.name }}</h4>
       
        {% if product.price %}
        <p class="price"> {% if not product.special %}
          {{ product.price }}
          {% else %} <span class="price-new">{{ product.special }}</span> <span class="price-old">{{ product.price }}</span> {% endif %}
          {% if product.tax %} <span class="price-tax">{{ text_tax }} {{ product.tax }}</span> {% endif %} </p>
        {% endif %} </div>
      <a href="#" onclick="order(),  return false;" class="btn-order">Заказать</a>
    </div>
	
  </div>
  {% endfor %} 
  
  <a href="index.php?route=product/category&path=62" class="brn-more">Посмотреть еще</a>
